{
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "serve": "",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "prepare:db": "cd apps/api && ./vendor/bin/sail up -d mysql",
    "migrate": "docker compose exec api php artisan migrate",
    "seed": "docker compose exec api php artisan db:seed"
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "prettier": "^3.2.5",
    "turbo": "^2.8.3"
  },
  "packageManager": "pnpm@8.15.6",
  "name": "with-vite-react"
}
