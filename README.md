# Simple Blog Monorepo (Laravel + React + TS)

Тестовое задание: Разработка блога с комментариями.
Стек: **Laravel 12 (PHP 8.3)**, **React 19 (Vite)**, **TypeScript**, **MySQL**.

---

## 🏗 Структура проекта
- `apps/api` — Backend API (Laravel).
- `apps/web` — Frontend SPA (React).
- `packages/` — Общие конфигурации Turborepo.

---

## 🐳 Вариант 1: Развертывание в Docker (Рекомендуется)

Этот способ изолирует окружение и не требует установленного PHP/Node на вашем компьютере.

### 1. Настройка переменных окружения
Скопируйте файлы настроек для обоих приложений:
```bash
cp apps/api/.env.example apps/api/.env
```
или для docker
```bash
cp apps/api/.env.docker apps/api/.env
```

### 2. Сборка и запуск контейнеров
```bash
docker compose up -d
```

### 3. Установка зависимостей и БД
   Выполните команды внутри контейнера бэкенда:
   bash
# Установка PHP-пакетов
```bash
docker compose exec api composer install
```
# Настройка приложения и БД
```bash
docker compose exec api php artisan key:generate
docker compose exec api php artisan migrate --seed
```

### Вариант 2: Локальное развертывание

# 1. Подключитесь к MySQL и создайте таблицу в базе данных **api**

# 2. Настройка и запуск Backend (API)
```bash
cd apps/api
composer install
php artisan key:generate
php artisan migrate --seed
php artisan serve --port=8080
```

# 3. Настройка и запуск Frontend (Web)
```bash
cd apps/web
pnpm install
pnpm dev
```

# 📡 API Endpoints (Порт 8080)
* GET /api/articles — Список статей.
* GET /api/articles/{id} — Статья с комментариями.
* POST /api/articles — Добавление статьи.
* POST /api/articles/{id}/comments — Добавление комментария.

# 🛠 Команды обслуживания
* docker compose down -v — Полная очистка контейнеров и томов базы данных.
* docker compose logs -f — Просмотр логов в реальном времени.
* npx turbo build — Сборка всего монорепозитория.


